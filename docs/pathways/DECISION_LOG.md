# Pathways Discovery App — Decision Log

Version: 0.4.0-phase1a-repair
Owner reviewed and authorized this log's 0.1.0-phase0-candidate revision. DEC-B1 through DEC-B10 are now **ACCEPTED** by explicit owner authorization. DEC-C1 through DEC-C7 are now **RESOLVED** per the owner's specific instructions, implemented in `contracts/` (see `contracts/CHANGELOG.md` for the exact diff) and traced in `docs/pathways/REQUIREMENT_TRACEABILITY.md`. DEC-E1 (Phase 1 stack) is **INSTALLED**, not merely resolved-on-paper -- see `docs/pathways/INTEGRATION_REGISTER.md` for exact versions and `docs/pathways/PHASE_STATUS.md` for full Phase 1 evidence. Phase 1 is complete; this session then stopped after it, per owner instruction. The owner subsequently reviewed the Phase 1 implementation at commit `5d51306` and authorized a targeted "PHASE 1A — FOUNDATION REPAIR ONLY" against six specific findings (§F below); Phase 2 was not started before, during, or after that repair.
Companion to `IMPLEMENTATION_CONTRACT.md`. Each row is one decision point. **Status** is one of `ACCEPTED` (already reflected in the pack's normalized specs/contracts, no further owner input required to build against it), `PROPOSED` (the pack itself flags this as needing explicit owner sign-off before it controls implementation), or `UNRESOLVED` (an open question this Phase 0 audit surfaced that the pack does not answer). **Blocks** indicates what this decision gates — nothing, Phase 1, Phase 3, Phase 4, or live launch.

## A. Decisions carried from the latest owner direction (ACCEPTED)

| ID | Decision | Source | Blocks |
|---|---|---|---|
| DEC-A1 | K-12 supported; grades 5-12 marketing emphasis; ELEMENTARY/MIDDLE/HIGH_SCHOOL/UNDETERMINED grade bands | Spec 01, Spec 06 §1 | Nothing |
| DEC-A2 | No mandatory email/account wall for the guest Discovery Report | Spec 05, Spec 06 §2 | Nothing |
| DEC-A3 | Public fit labels limited to WORTH_EXPLORING / WORTH_EXPLORING_WITH_CONSIDERATIONS / MORE_INFORMATION_HELPFUL; no percentages, no "Strong Potential Fit" | Spec 04, Spec 06 §3 | Nothing |
| DEC-A4 | No live Pathways school, no automatic B10 fallback, no named provider, no assured local eligibility in Discovery V1 | Spec 01, 03, 04; Spec 06 §4 | Nothing |
| DEC-A5 | AI limited to two writing slots (R01 summary, R02 insight); never chooses pathways; report fully functional with zero AI keys | Spec 05, 08 (prompt); Spec 06 §5 | Nothing |
| DEC-A6 | Provider/business/legal facts require explicit configuration/approval before assertion; mockups are visual reference only, never a source of copyable claims | Spec 06 §6; prompts/03 | Nothing |

## B. Builder-review corrections proposed in Specification 06 (ACCEPTED — owner authorization received)

| ID | Decision | Already implemented in contracts? | Blocks |
|---|---|---|---|
| DEC-B1 | Add DISC_031 (school-change preference), DISC_032 (unavailable times), DISC_033 (delivery interest), DISC_034 (subject advancement interest), DISC_E04 (real adult availability) | Yes — all five present in `question-bank.json` | Phase 3 |
| DEC-B2 | Optional nickname/display name (DISC_001) and optional ZIP (part of DISC_004) | Yes | Phase 3 |
| DEC-B3 | Canonical IDs only; legacy P-series/SUP_/OPP_/ambiguous S09 are import-only aliases | Yes — `legacy-aliases.json` | Phase 1 (schema), Phase 4 |
| DEC-B4 | Capped, grouped scoring; two-group display gate; no score-only public threshold | Yes — `scoring-policy.json` | Phase 4 |
| DEC-B5 | No unasked inferences (travel frequency, math-ahead, afternoon-unavailable, supervision level) | Yes — Spec 02 "Normalization used by rules" section is explicit about what does *not* get inferred | Phase 3, Phase 4 |
| DEC-B6 | Graduation concern ≠ missing credits; college desire ≠ AP/dual-enrollment readiness; credit review from grade 9 | Yes — reflected in `rules.json` (`HS_001`-`HS_004`) and fixtures `FX05`/`FX10` | Phase 4 |
| DEC-B7 | No blanket "private-online is superior" or "hybrid = both live-online and on-campus" assumption | Yes — Spec 03/04 prose; no rule encodes such a ranking | Phase 4 |
| DEC-B8 | Rule engine / content library / reported learning preferences kept distinct; no "learning styles" diagnostic language | Yes — Spec 06 §14; content library uses hedged template language | Phase 5 |
| DEC-B9 | Report storage/versioning subordinate to an approved retention/deletion policy | Partially — architecture anticipates this (`ReportSnapshot`, retention-governed `ProfileRevision`); actual retention *period* is a launch decision (see Section D) | Phase 1 (schema), launch (values) |
| DEC-B10 | Advisory/sales workflow architecturally independent of educational rank | Yes — Spec 04 "Independent lead workflow"; engine input contract excludes lead fields entirely | Phase 1, Phase 4, Phase 7 |

**Owner action:** received. Accepted as written, subject to the clarifications in the owner's authorization message, none of which altered DEC-B1-B10's substance (the clarifications applied to DEC-C1-C7 instead). Closed.

## C. New findings from this Phase 0 audit (RESOLVED per owner instruction)

These came from manually cross-checking all 52 rules against all 38 questions and the full taxonomy (10 base models, 10 overlays, 12 supports, 18 opportunities, 20 review signals). Full explanation is in `IMPLEMENTATION_CONTRACT.md` §3.3; this table is the tracking record.

| ID | Finding | Owner-directed resolution | Implemented at | Status |
|---|---|---|---|---|
| DEC-C1 | `ncaa_interest` had no canonical question ID. | Formal question `DISC_020A` added exactly per owner's specified wording, values, labels, and show-when (grade MIDDLE/HS AND athletics branch active AND `college_athletics_interest` DEFINITELY/POSSIBLY; never K-4). Explicit-UNKNOWN-vs-hidden distinction formalized as a `global_rules` entry. | `contracts/question-bank.json` | RESOLVED |
| DEC-C2 | `RESEARCH` advancement interest had no rule. | New rule `ADV_007`: `advancement_interests contains RESEARCH` (MIDDLE/HS) → overlay `O02`, opportunity `OP06`, review `REV_ADVANCEMENT_READINESS`. Empty `score_effects` per instruction not to change base-model scores. Age-appropriateness already satisfied because K-4's DISC_022 value subset excludes RESEARCH entirely. | `contracts/rules.json`, `contracts/question-bank.json` | RESOLVED |
| DEC-C3 | Six opportunities unreachable. | `OP06` made reachable via `ADV_007`. `OP07`, `OP09`, `OP12`, `OP13`, `OP15` explicitly marked `RESERVED` in `taxonomy.json` (kept, described, forbidden from display, exempted from reachability-check failure) rather than given fabricated triggers, since no question currently captures their underlying interest. | `contracts/taxonomy.json` | RESOLVED |
| DEC-C4 | `S01`, `S08` unreachable. | `S01` marked `BASELINE` (potential service category, not rule-gated; actual availability comes from service configuration, never implies an assigned/purchased advisor). New signal-only rule `ADV_008`: `college_intent in [DEFINITELY, PROBABLY]` (MIDDLE/HS) → support `S08`. Empty `score_effects`; never K-4 (branch already excludes it). | `contracts/rules.json`, `contracts/taxonomy.json` | RESOLVED |
| DEC-C5 | `REV_AGE_GRADE_CONTEXT`, `REV_SERVICE_AVAILABILITY` had no trigger. | New rule `GRADE_001`: `grade_band = UNDETERMINED` → `REV_AGE_GRADE_CONTEXT` (empty `score_effects`; neutral clarification need, never a risk score or presumption of retention/acceleration/disability/eligibility). `REV_SERVICE_AVAILABILITY` explicitly documented as **outside** the rules engine by design — an operational/service-configuration concern resolved by the Phase 6 consultation adapter, affecting only the report CTA, never educational scoring or candidate display. | `contracts/rules.json`, `contracts/taxonomy.json` | RESOLVED |
| DEC-C6 | `HOME_003`/`PAR_002` redundant. | `HOME_003` marked `RETIRED` with `replacement_rule_id: PAR_002` and a `retired_reason` documenting the direct comparison confirming retirement changes no score and removes no unique behavior (kept in the file, not deleted, for historical explainability; Phase 4 engine must skip RETIRED rules). `PAR_002` given `dedup_key: HOME_MANAGEMENT_TRADEOFF` for report-layer deduplication. | `contracts/rules.json` | RESOLVED |
| DEC-C7 | Thin `cost`/`context` groups. | Display gate left unchanged, exactly as instructed — no artificial score contributions added. Three regression fixtures added instead: `FX15_AFFORDABILITY_ONLY`, `FX16_ENVIRONMENT_ONLY`, `FX17_MOSTLY_UNKNOWN`, each asserting that a zero/minimal-card, `LIMITED_INFORMATION` outcome is valid and correct. | `fixtures/golden-profiles.json` | RESOLVED |

## D. Decisions needed only before live launch (not blockers to Phases 1-9 against synthetic data)

| ID | Decision | Owner input needed |
|---|---|---|
| DEC-D1 | Legal operating entity, final brand, published privacy notice/terms, contact address | Owner/legal |
| DEC-D2 | Which services/grades/markets are actually staffed; consultation price (if any), duration, scheduling windows | Owner/operations |
| DEC-D3 | Auth/email/scheduler provider selection and approved data-processing terms | Owner + engineering |
| DEC-D4 | Guest/saved-record retention periods, deletion procedure, backup/log handling | Owner + engineering |
| DEC-D5 | AI provider selection, data-handling approval, cost ceiling, whether AI ships at initial launch at all | Owner |
| DEC-D6 | Media usage rights, verified advisor credentials, any claimed affiliations | Owner |

Until each of these is supplied, the corresponding feature returns an honest unavailable/request state. None is invented or defaulted anywhere in this contract.

## E. Phase 1 stack (RESOLVED — owner-approved)

| ID | Decision | Status |
|---|---|---|
| DEC-E1 | Next.js App Router + TypeScript + PostgreSQL + Drizzle (schema/migrations) + Better Auth (authentication groundwork, email-link, live sending disabled) + Zod (runtime validation) + pnpm with a committed lockfile. Current mutually-compatible stable releases only; no beta/canary/RC pins. | RESOLVED — exact installed versions recorded in `INTEGRATION_REGISTER.md` once installed. |

## F. Phase 1A repair findings (owner review of commit `5d51306`, RESOLVED this repair)

The owner reviewed the Phase 1 implementation at commit `5d51306` and issued a 7-section repair authorization scoped explicitly to correction, not redesign ("Do not restart planning, redesign the architecture, change the business model, or begin Phase 2"). Each finding below is now RESOLVED; see `docs/pathways/PHASE_STATUS.md` "Phase 1A repair evidence" for the full finding-to-fix-to-test table and actual command output, and `docs/pathways/REQUIREMENT_TRACEABILITY.md` §10 for the requirement re-trace (N8/N9/N10).

| ID | Finding | Resolution | Implemented at | Status |
|---|---|---|---|---|
| DEC-F1 | CI workflow trigger/permission/env-mode scope and its "must fail if the test DB or integration suite is unavailable" behavior had not been independently verified, and risked a required job silently passing without real integration coverage. | Rewrote `.github/workflows/ci.yml` (explicit `main`/`claude/**` push+PR triggers, `contents: read`, per-step `NODE_ENV`, disposable `postgres:16` service) and added two independent fail-loud mechanisms: the `REQUIRE_TEST_DATABASE` env gate in `tests/helpers/db.ts` and `scripts/assert-integration-tests-ran.ts`. Actual GitHub Actions dispatch result recorded in `PHASE_STATUS.md`, not a local simulation alone. | `.github/workflows/ci.yml`, `tests/helpers/db.ts`, `scripts/assert-integration-tests-ran.ts` | RESOLVED |
| DEC-F2 | Magic-link initiation had no explicit block while `EMAIL_MODE=UNCONFIGURED`; a request could reach Better Auth's real handler and create a genuine, undeliverable verification row. Documentation risked overclaiming verified sign-in from a health/get-session check alone. | `hooks.before` middleware in `src/auth/config.ts` blocks `/sign-in/magic-link` before the handler (and `createVerificationValue`) runs, for every `EMAIL_MODE` currently implemented; zero verification rows created, no sensitive value ever logged (verified by console spies). Full lifecycle proven correct separately via an isolated, test-only in-memory delivery adapter. Doc comment in `app/api/auth/[...all]/route.ts` corrected. | `src/auth/config.ts`, `app/api/auth/[...all]/route.ts`, `tests/magic-link.test.ts` | RESOLVED |
| DEC-F3 | Advisor case access was not composed (staff role AND assignment); the guardian single-record check did not match the list function's soft-delete filter; no reusable server-only principal boundary existed; `server-only` import boundaries were incomplete. | Composed advisor checks (`CASE_ACCESS_ROLES`, no admin bypass, documented); fixed guardian soft-delete parity; added `src/server/principal.ts`; added `import "server-only"` to db/env/access-control/session/principal modules with a Vitest-only alias that does not weaken the real build's enforcement. | `src/server/access-control.ts`, `src/server/principal.ts`, `vitest.config.ts`, `tests/access-control.test.ts`, `tests/principal.test.ts` | RESOLVED |
| DEC-F4 | `dto.ts`'s public report `sections` field was typed `unknown` and passed the stored value straight through with no runtime validation of its nested structure. | Added `publicReportSectionsSchema` (R01-R07, `z.strictObject` at every level including nested cards and arrays) derived from `report-contract.json`; `toPublicReportSnapshot` now throws `InvalidReportContentError` on any mismatch rather than ever serving unvalidated content. Explicitly not the Phase 5 report generator/UI. | `src/server/dto.ts`, `tests/dto.test.ts` | RESOLVED |
| DEC-F5 | Session expiry was a hardcoded constant; `guestSessionCookieOptions` used `NODE_ENV` as a stand-in for "is this a live deployment"; a doc comment implied HTTPS was checked when only `NODE_ENV` was. | Added `DEPLOYMENT_MODE`/`GUEST_SESSION_TTL_MINUTES`/`LIVE_DEPLOYMENT_APPROVED` to `src/env.ts` (LIVE mode refuses to start without explicit approval and TTL); `src/server/session-config.ts` derives the DB expiry and cookie `maxAge` from the same configured value; corrected the misleading comment. No retention/deletion subsystem was built — this is session expiry, not data deletion, per the owner's explicit instruction. | `src/env.ts`, `src/server/session-config.ts`, `src/server/session.ts`, `tests/env.test.ts`, `tests/session-config.test.ts`, `tests/session.test.ts` | RESOLVED |
| DEC-F6 | The 4 evidence docs did not yet record this repair's original-state/finding/fix/test/outcome detail. | This section, plus `PHASE_STATUS.md` "Phase 1A repair evidence", `REQUIREMENT_TRACEABILITY.md` §10, and this row's own presence in `INTEGRATION_REGISTER.md`'s Authentication/Testing/CI/Hosting rows. | `docs/pathways/{PHASE_STATUS,REQUIREMENT_TRACEABILITY,INTEGRATION_REGISTER,DECISION_LOG}.md` | RESOLVED |

**Owner action:** none required — this repair was fully scoped and authorized in the owner's original 7-section message; no new decision point was opened by it. Phase 2 remains not started, per that authorization's explicit stop instruction.

## G. Phase 3 engineering decisions (Discovery Profile — RESOLVED this phase, not blockers to Phase 4)

Genuine new engineering interpretations/decisions made while building the conditional Discovery Profile, per the phase's own requirement to record these (not routine code). See `docs/pathways/PHASE_STATUS.md` "Phase 3 evidence" for full implementation detail.

| ID | Decision | Reasoning | Implemented at | Status |
|---|---|---|---|---|
| DEC-G1 | Resume/edit position within the questionnaire is never stored as its own column or field. | It is always re-derived fresh from `computeActiveFlow()`/`computeProgress()` over the stored `draftAnswers` on every load, which makes position/answer drift structurally impossible (a stored position could otherwise point at a stage a later branch change has since hidden). | `src/lib/discovery/progress.ts`, `src/server/discovery-draft.ts` | RESOLVED |
| DEC-G2 | `desired_primary_change` (DISC_008) values `CHALLENGE` and `COLLEGE_COURSES` are read as "advancement-oriented" for the `ADVANCEMENT_INTEREST_OR_REPORTED_AHEAD_OR_MIXED` branch predicate. | The branch's own name requires classifying DISC_008's free-form-feeling enum into "counts as advancement interest" vs. not; the pack does not enumerate this composite branch's exact DISC_008 subset. `CHALLENGE` and `COLLEGE_COURSES` were judged the two values a parent selecting them is genuinely signaling an advancement-oriented change, consistent with the same branch's other two triggers (`discovery_reasons` containing `ACADEMIC_ACCELERATION`/`ADVANCED_COURSES`, or `reported_academic_position` being `AHEAD`/`MIXED`). | `src/lib/discovery/branching.ts` | **OWNER APPROVED AS IMPLEMENTED** (Phase 3A, 2026-09-23) — explicitly not to be broadened to `OUTSIDE_OPPORTUNITIES` or any other value. |
| DEC-G3 | Submission idempotency uses a deterministic content-hash key (`sha256` of a canonicalized `{sessionId, questionBankVersion, rawAnswers}`), not a client-supplied nonce, enforced by a partial unique Postgres index. | A content-hash key requires zero additional client-side state, collapses identical-content retries to the same row automatically, and makes any genuine edit naturally produce a new key — a client nonce would need its own generation/storage/replay-protection design for the same guarantee. A Postgres `SAVEPOINT` (nested transaction) around the risky INSERT was required because Postgres aborts the entire outer transaction on any statement error, which would otherwise block the idempotent-replay `SELECT` from running in the same transaction. | `src/server/discovery-draft.ts` (`computeSubmissionIdempotencyKey`, `submitDiscoveryProfile`), `drizzle/0001_harsh_legion.sql` | RESOLVED |
| DEC-G4 | `pnpm build` in this development sandbox cannot be completed successfully; the actual GitHub Actions CI dispatch is treated as the authoritative build verification for this and future phases, not the local command. | Reproduced identically on a fully clean `HEAD` checkout (zero Phase 3 changes) and after a fully fresh, registry-forced `node_modules` reinstall — ruling out both this phase's code and local dependency corruption. The exact same commit's actual GitHub Actions run completed its build step successfully on GitHub's own runner. This is documented as a sandbox-environment anomaly (likely a Turbopack/Next 16.3.5 native-binary compatibility issue specific to this container), not a code defect requiring a fix within this phase's scope. | N/A (environment observation) | RESOLVED — worked around by relying on the real CI build result |
| DEC-G5 | A homepage `?interest=` marketing hint is shown only as a visual preselection on `discovery_reasons` and is never written to the stored draft until the parent performs a genuine interaction with that control (check/uncheck), even if the hint's value is never touched. | Chosen as the more conservative reading of "an editable preselection, not a silently-confirmed answer" — a hint the parent never looks at is arguably not yet "their answer." Flagged for explicit owner confirmation rather than assumed. | `src/server/discovery-draft.ts`, `src/components/discovery/DiscoveryQuestionnaire.tsx` | **OWNER CLARIFIED** (Phase 3A, 2026-09-23) — superseded by DEC-G6 below; see that row for the confirmed behavior. |

## H. Phase 3A — owner clarification patch (RESOLVED, not a Phase 4 start)

The owner reviewed Phase 3 (commit `0051e66`) and issued a scoped clarification patch against exactly DEC-G2 and DEC-G5, explicit that no other Phase 3 behavior, the marketing-interest allowlist, canonical DISC_006 values, branching semantics, recommendation rules, normalization, session/`ProfileRevision`/idempotency architecture, or visual design was to change, and that Phase 4 remains not authorized.

| ID | Decision | Reasoning | Implemented at | Status |
|---|---|---|---|---|
| DEC-G6 | A homepage `?interest=` marketing hint remains a non-persistent visual preselection when the Discovery session is created and while the parent has not yet reached/confirmed DISC_006. The moment the parent presses **Continue** from the GOALS (DISC_006) screen, whatever is then visibly selected — the untouched hint, a changed selection, or the hint plus an added reason — is persisted as the real `discovery_reasons` answer before advancing. The parent is never required to uncheck/recheck an already-correct preselection merely to confirm it. Abandoning the questionnaire before reaching/confirming DISC_006 (e.g. pressing Back, closing the tab) never converts the hint into a completed answer, and branch activation (e.g. the Athletics stage) is driven only by the confirmed, persisted answer, never by the unconfirmed hint. | The owner's explicit clarification of DEC-G5: Continue while a visible selection remains selected is itself the parent's confirmation of what they see, so no artificial uncheck/recheck gesture should be required. Implemented as a client-side-only change (`DiscoveryQuestionnaire.tsx`'s `handleContinue`): on the GOALS stage, if a hint is still active and `discovery_reasons` has a value, that exact value is explicitly (re-)saved via the existing `saveAnswerAction`/`saveDraftPatch` path before navigating; if the save fails, the parent stays on GOALS with the existing error/retry state rather than silently advancing past an unpersisted required answer. No change was made to `saveDraftPatch`, `startDiscoverySession`, `resumeOrStartDiscoverySession`, or any branch predicate — the server-side hint/answer separation and the "branches read only persisted `rawAnswers`" guarantee were already structurally true and needed no repair. Any real checkbox interaction already committed immediately before this patch and continues to; this change only closes the "never touched at all" gap. | `src/components/discovery/DiscoveryQuestionnaire.tsx` (`handleContinue`, `commit` now returns success/failure) | RESOLVED |

**Owner action:** none further required — DEC-G2 is approved as implemented; DEC-G5 is clarified and superseded by DEC-G6. Neither opens a new Phase 4 decision point.

## I. Phase 3B — Vercel Preview database runtime repair (in progress; blocked on a hosted database)

The owner reproduced a real runtime failure testing the deployed Vercel Preview: `POST /discover` returned HTTP 500, with the runtime log showing `connect ECONNREFUSED 127.0.0.1:5432` on `INSERT INTO discovery_session`. Full diagnosis, code changes made so far, and the remaining blocker are recorded in `docs/pathways/PHASE_STATUS.md` "Phase 3B evidence" and `docs/pathways/INTEGRATION_REGISTER.md` "Phase 3B: Vercel Preview database runtime."

| ID | Decision | Reasoning | Implemented at | Status |
|---|---|---|---|---|
| DEC-G7 | A deployed, non-`LOCAL` environment (`PREVIEW` or `LIVE`) refuses to start **at genuine runtime** if `DATABASE_URL` resolves to a loopback host (`localhost`/`127.0.0.1`/`::1`), rather than only failing later at the first real query -- but the check is explicitly skipped while `next build` itself is evaluating the module (`process.env.NEXT_PHASE === "phase-production-build"`). | The failure this repair diagnosed is exactly the class of bug this prevents by construction: a placeholder/misconfigured `DATABASE_URL` reaching a deployed serverless environment, where `localhost` never refers to a real database and the resulting error is an opaque, hard-to-diagnose `ECONNREFUSED` at request time instead of an actionable message at startup. `LOCAL` is deliberately exempt, since a developer's own machine legitimately runs Postgres on `localhost`. **A real regression was caught and fixed within this same repair**: the first version of this check had no build-phase exemption, and the actual Vercel Preview build for that commit failed outright (`pnpm run build` exited 1) because `next build` imports every route module -- including `src/env.ts` -- during its page-data-collection step, with the *same* Preview `DEPLOYMENT_MODE=PREVIEW` and placeholder `DATABASE_URL` env vars the deployed function would see, even though nothing is actually serving a request at that point. Fixed by additionally requiring `NEXT_PHASE !== "phase-production-build"`; see `PHASE_STATUS.md` "Phase 3B evidence" for the actual Vercel deployment result this fix was verified against. | `src/env.ts` (`isLoopbackDatabaseUrl`, checked after the existing LIVE-mode approval/TTL gates) | RESOLVED |
| DEC-G8 | `/discover` and its subroutes get a dedicated `error.tsx` boundary showing a fixed, neutral "Discovery is temporarily unavailable" message with a retry action, rather than leaving Next.js's generic framework error page as the failure experience. | The owner's own instruction explicitly separated "do not hide infrastructure errors as fake success" from "evaluate whether a graceful failure state is warranted" -- this satisfies the second without violating the first: the page still honestly indicates something is wrong and offers no fake success, it simply does not show a raw, framework-generated crash page or any of the underlying error's real detail. Verified that the message shown never includes the database host, SQL, or a credential -- confirmed directly via a local reproduction pointing at a genuinely unreachable database port, and further verified against Next.js's own documented behavior that a thrown Server Component/Action error's message is redacted to a generic message + digest in a production build. | `app/discover/error.tsx` | RESOLVED |
| DEC-G9 (open) | Provisioning a real, hosted, remotely-accessible PostgreSQL instance for the Vercel PREVIEW environment requires the owner (or someone with Vercel dashboard access) to act -- this session's Vercel API access cannot list, create, or otherwise manage marketplace integration configurations (`403 Forbidden` on every call attempted, including after being asked to retry), and no paid or free external service was provisioned without that authorization, per this repair's explicit instruction. | This is a genuine access/authorization boundary, not an engineering choice: Vercel's marketplace integration flow appears to require a human completing its own dashboard consent step, which is outside what this session's API credentials can perform on the owner's behalf. | N/A (owner/dashboard action required) | **OPEN -- blocks the remainder of Phase 3B** |

**Owner action requested:** supply a real PostgreSQL connection string for the Vercel PREVIEW environment (via the project's Storage tab connecting a marketplace provider such as Neon or Supabase, both free-tier, or any other already-existing hosted instance) so DEC-G9 can be closed and the remaining Phase 3B steps (env var, migrations, schema verification, real end-to-end deployed test, database-side evidence) can proceed. Neither DEC-G7 nor DEC-G8 requires further owner input.

## J. Phase 3C — Preview Discovery Demo Mode (`/discover/demo`, database-free, not a Phase 4 start)

DEC-G9 (Section I) remains open: the Vercel PREVIEW environment still has no hosted PostgreSQL database. This section adds a strictly isolated, database-free way for the owner to click through the full Discovery questionnaire experience on the actual Vercel Preview while DEC-G9 stays open -- it does not close DEC-G9, does not touch the real server-backed Discovery architecture, and does not start Phase 4.

| ID | Decision | Reasoning | Implemented at | Status |
|---|---|---|---|---|
| DEC-G10 | `/discover` and everything under it must never statically import `@/db/client`, `@/server/session`, or `@/server/discovery-draft` at module scope in a page or `"use server"` action file; each such import is instead an `await import(...)` placed inside the function body that actually needs it. | Independently discovered (not owner-reported) while building this section: Next.js eagerly evaluates a route's entire static import graph -- including a `"use server"` action module referenced by a `<form>`, and any module a Server Component statically imports -- merely to RENDER a page, before any action is invoked and before that page's own function body executes. Since `src/db/client.ts` and `src/server/session.ts` both transitively reach `src/env.ts`'s `loadEnv()` at module-evaluation time, and DEC-G7's loopback check throws for exactly the Vercel Preview's current `DEPLOYMENT_MODE=PREVIEW` + placeholder `DATABASE_URL`, a static top-level import of either module anywhere in `/discover`'s import graph made a plain `GET /discover` crash outright (a blank HTTP 500, never reaching `app/discover/error.tsx`, since a module-evaluation-time throw happens outside React's render lifecycle entirely) -- which would have made the new "Preview the Discovery Experience" CTA below unreachable on the one environment it exists for. Deferring each import into the function body that uses it moves the potential throw to actual invocation time, where it *is* caught by the existing `error.tsx` boundary (verified via two rounds of local reproduction of the exact Preview configuration, before and after the fix, plus the automated regression in `tests/discovery-demo-preview-repro.test.ts`, which spawns a real `next dev` process with this exact env and asserts both `/discover` and `/discover/demo` render 200). | `app/discover/actions.ts`, `app/discover/profile/actions.ts`, `app/discover/profile/page.tsx`, `app/discover/report/page.tsx` | RESOLVED |
| DEC-G11 | `/discover/demo` reads `process.env.DEPLOYMENT_MODE` directly (the raw, unvalidated value) to decide whether to show the Preview Demo Mode CTA, never the validated `env` export from `@/env`. | The validated `env` export's own `loadEnv()` is exactly the function that throws for the Preview's current configuration (DEC-G7) -- importing it, even lazily inside the Server Component's function body, would make `/discover` crash before the CTA it is being asked to render could ever appear, defeating the point of this section. A plain string comparison needs no validation and therefore never touches that module. | `app/discover/page.tsx` | RESOLVED |
| DEC-G12 | Preview Demo Mode's answer state is held only in the Client Component's React memory (`DiscoveryDemoQuestionnaire.tsx`); every field commit sends the ENTIRE current in-memory answers object to a stateless `"use server"` function (`app/discover/demo/actions.ts`) that recomputes progress/questions/review sections using only the existing, unmodified `@/lib/discovery` registry/branching/normalization/validation/labeling functions, then echoes the result back to the client. No session, cookie, draft row, or second question bank exists anywhere in this path. | The instruction's hard constraint was that demo and real Discovery must interpret answers identically with zero duplicated branch/validation logic, and that the demo must not require a database connection. A stateless full-snapshot recompute against the exact same canonical functions the real, DB-backed flow already calls guarantees byte-for-byte identical interpretation by construction, rather than by parallel maintenance of a second implementation. `src/lib/discovery/marketing-hint.ts` (new) extracts the homepage-interest-hint mapping out of the DB-touching `src/server/discovery-draft.ts` into a dependency-minimal module with no `@/db` import anywhere in its graph, so the demo can share it without pulling in anything DB-touching; `discovery-draft.ts` re-exports it for full backward compatibility with existing callers/tests. | `app/discover/demo/actions.ts`, `app/discover/demo/page.tsx`, `src/components/discovery/DiscoveryDemoQuestionnaire.tsx`, `src/lib/discovery/marketing-hint.ts` | RESOLVED |
| DEC-G13 | `/discover/demo`'s Client Component applies every answer to its own in-memory state optimistically and synchronously (before awaiting the stateless recompute call), using a `ref` (not the render-closure state variable) as the base for each server call, and never lets the server's echoed answer snapshot overwrite what the client already holds. | Discovered and fixed during this section's own Playwright verification: without the optimistic update, a controlled radio/checkbox visibly reverted to unchecked for the round-trip duration (a real, user-facing flicker, not just a test artifact); without the ref (i.e. reading `rawAnswers` from the render closure), two commits started before either resolves (e.g. a text field's blur firing immediately before a sibling radio's click) could have the later-resolving call's full-snapshot response silently overwrite the earlier one's already-applied answer. Mirrors the real `DiscoveryQuestionnaire.tsx`'s own optimistic `setLocalAnswers` pattern, adapted for having no server-side draft row to fall back on. | `src/components/discovery/DiscoveryDemoQuestionnaire.tsx` (`commit`, `applyLocally`, `rawAnswersRef`) | RESOLVED |

This section does not close DEC-G9 or change anything about Section I; the real, database-backed Discovery architecture (`/discover/profile`, `DiscoverySession`, `StudentPathwayRecord`, `ProfileRevision`, server-backed drafts, idempotent submission) is untouched and remains exactly as blocked on a hosted Preview database as before. **Phase 4 is not authorized by this section and has not been begun.**

## K. Phase 3E — Discovery calibration (RESOLVED, not a Phase 4 start)

Owner-approved revision, full spec at `docs/pathways/DISCOVERY_CALIBRATION_SPEC_V2.md`. Hard owner decision: Discovery is not a diagnostic intake form -- it must read as positive/possibility-oriented throughout, and no interpretive "what we're hearing" moment may appear between Questions and Review/Complete. This section calibrates the existing Phase 3 build's wording, options, branching and normalized facts; it does not implement the Phase 4 recommendation engine, the final Discovery Report, or any scoring evaluator.

| ID | Decision | Reasoning | Implemented at | Status |
|---|---|---|---|---|
| DEC-H1 | Added a new, generic `grade_band_allowed_values` mechanism to the question schema (optional `Record<GradeBand, string[]>`), consulted by `present.ts` in place of the full `allowed_values` list whenever it is defined for a question. | Confirmed by direct code inspection that prior `matching_use` prose claiming "K-4 hides X/Y/Z" was never actually enforced anywhere in `present.ts` (which mapped over the unconditional `allowed_values` list) -- a real, pre-existing gap, not a Phase 3E regression. `allowed_values` still lists every value, including legacy/retired ones, so a historical raw answer keeps validating and Review keeps reading it back correctly; only the *new-entry* option set is grade-tiered. | `src/lib/contracts/schemas.ts`, `src/lib/discovery/present.ts`, `contracts/question-bank.json` (DISC_006/008/012/022/026/034) | RESOLVED |
| DEC-H2 | Cost preference (`cost_preference`/DISC_028) belongs to PRACTICAL FEASIBILITY, not EDUCATIONAL ALIGNMENT (`reason_type: "FEASIBILITY"` added to the rules schema). `COST_001` (which directly added `{B02: 2, B03: 3, B04: 3}` to base-model scores for `PREFER_TUITION_FREE`) is retired with no replacement scoring rule. Cost still reaches a future report only through the existing `REV_COST_ALIGNMENT` postprocess signal, which surfaces it as a discussion point rather than inflating a model's score. | A family's preference for tuition-free options is not evidence that a tuition-free base model is a *better educational fit* for their student -- `COST_001`'s direct `score_effects` was exactly this anti-pattern, silently mixing a feasibility preference into an alignment score. Section 12 of the calibration spec requires these two concepts never be collapsed into one number; Phase 3E creates/documents the distinction but deliberately does not implement the Phase 4 evaluator this distinction will govern. | `contracts/rules.json` (`COST_001` status), `contracts/scoring-policy.json` (postprocess_rules), `src/lib/contracts/schemas.ts` (`reason_type`) | RESOLVED |
| DEC-H3 | DISC_022's `DUAL_ENROLLMENT` and `COLLEGE_COURSES` are merged into one family-facing concept and one canonical current value, `COLLEGE_LEVEL_COURSES`, targeting taxonomy opportunity `OP05` alone. `ADV_003` (the `DUAL_ENROLLMENT`-only rule targeting `OP04`) is retired with `replacement_rule_id: "ADV_004"`; `ADV_004`'s own condition is updated from `COLLEGE_COURSES` to `COLLEGE_LEVEL_COURSES`; `OP04` moves `ACTIVE` -> `RETIRED`. Both legacy values alias to the canonical one only in *effective* answers (`contracts/legacy-aliases.json`), never in raw storage. | The two technical values described the same family-facing idea ("college-level or dual-enrollment courses") and previously risked producing two near-duplicate opportunity cards for one underlying interest. `OP05` ("College-course exploration") was kept over `OP04` ("Dual-enrollment exploration") as the better semantic fit for the merged concept. | `contracts/question-bank.json` (DISC_022), `contracts/legacy-aliases.json`, `contracts/rules.json` (`ADV_003`, `ADV_004`), `contracts/taxonomy.json` (`OP04`) | RESOLVED |
| DEC-H4 | Taxonomy opportunities `OP07` (Internship), `OP09` (Industry-credential), `OP12` (Entrepreneurship) move `RESERVED` -> `ACTIVE`, each newly reached by one added rule (`ADV_011`/`ADV_012`/`ADV_013`) matching a new DISC_022 canonical value (`WORK_BASED_LEARNING`/`INDUSTRY_CREDENTIALS`/`ENTREPRENEURSHIP`). `OP13`/`OP15` remain the only forever-RESERVED opportunities (no capturing question exists for either). Two more previously dead-end DISC_022 values, `ENRICHMENT` and `CHALLENGING_COURSEWORK`, are wired via new rules `ADV_009`/`ADV_010` to existing active opportunities `OP18`/`OP01` rather than new ones, since both express "more than the standard curriculum" at their respective grade tiers. | These three taxonomy entries were reserved at Phase 0 specifically because "no question captures this yet" (DEC-C3); DISC_022's Phase 3E rework genuinely captures them now, so the Phase 0 reservation's own stated condition for staying reserved no longer holds. All five new rules have empty `score_effects` (interest signals only, consistent with every other DISC_022 rule). | `contracts/rules.json` (`ADV_009`-`ADV_013`), `contracts/taxonomy.json` (`OP07`, `OP09`, `OP12`, `constraints`) | RESOLVED |
| DEC-H5 | DISC_E02/DISC_E04's branch (daytime support person/availability) is renamed from the retired `ELEMENTARY_OR_HOME_BASED_INTEREST_WITH_SUPPORT_NEED` to `HOME_OR_REMOTE_SUPPORT_CONTEXT`, which additionally requires home/remote learning to actually be under consideration (a homeschool/online discovery reason, or a home/remote value already selected in `desired_delivery`) before the elementary-grade-band or support-need condition can activate it. | A genuine bug fix, not just a rename: the retired predicate asked *every* elementary family about daytime home supervision regardless of whether home/remote learning was ever mentioned -- including a family staying in traditional school purely for enrichment reasons, which the calibration spec explicitly calls out as incorrect. | `src/lib/discovery/branching.ts` (`isHomeOrRemoteSupportContext`) | RESOLVED |
| DEC-H6 | DISC_032's branch (`SCHEDULE_CONSTRAINT_CONTEXT`) and the `schedule_flexibility_need` derived fact both read the "an already-stated schedule-intensive context exists" signal from `discovery_reasons` (`ATHLETICS`/`TRAVEL`/`ARTS` -- always active, `show_when: "ALL"`), not from `flexibility_reasons` (DISC_014). | Discovered while writing this phase's own test coverage (`tests/discovery-calibration.test.ts`): `flexibility_reasons` is itself gated behind `isFlexibilitySomewhatOrHigher`, so for a family who rated flexibility `NOT_IMPORTANT` (or hasn't answered DISC_013 at all) it can never carry a raw value regardless of what the test data supplied -- making the "OR meaningful schedule-intensive context already exists" half of the intended broadened branch a dead condition that could never actually fire. `discovery_reasons`, being always active, is the only field that can supply this signal independently of the DISC_013 answer. Caught and fixed within this same phase, before commit. | `src/lib/discovery/branching.ts` (`isScheduleConstraintContext`), `src/lib/discovery/normalization.ts` (`deriveScheduleFlexibilityNeed`) | RESOLVED |

**Owner action:** none required. Phase 4 (the recommendation engine, scoring evaluator, and final Discovery Report) remains not authorized and was not started by this section.

## L. Phase 3F — Discovery UX simplification (RESOLVED, not a Phase 4 start)

Owner manual review of the Phase 3E build (commit `a28aa4b`) requested a targeted simplification pass: shorter wording, fewer visible options per question (8-12 target, never mechanical), a better Learning-section order, and a lightweight "Other" escape hatch on five broad questions. Full spec at `docs/pathways/DISCOVERY_UX_SIMPLIFICATION_PHASE3F.md`.

| ID | Decision | Reasoning | Implemented at | Status |
|---|---|---|---|---|
| DEC-L1 | `discovery_reasons` (DISC_006) is left over the 12-option guideline (15-18 depending on grade band) rather than consolidated in this phase. | The instruction's field-by-field wording/option directives (sections 3-15) named every other long-list question explicitly but not this one, and DISC_006 is read directly by five branch predicates and one rule condition (`ENV_001`) -- consolidating its values would mean redesigning branching.ts/rules.json well beyond a wording/option pass. Documented as a named, reviewable exception (`tests/discovery-option-count-qa.test.ts`'s `DOCUMENTED_EXCEEDING_QUESTIONS` allowlist) rather than silently exempted, so the standing QA test still catches any *other* question drifting past 12. | `tests/discovery-option-count-qa.test.ts` | RESOLVED |
| DEC-L2 | DISC_012 (preferred_learning_environment) precedes DISC_011 (learning_support_pattern) in display and evaluation order, but DISC_011 stays `show_when: "ALL"` rather than becoming a DISC_012-gated follow-up. | The instruction's own escape hatch ("if hiding DISC_011 for some learning modes would weaken support_structure_need... it is acceptable to keep DISC_011 broadly active") applies directly: `support_structure_need`'s only primary signal is DISC_011, and most DISC_012 answers (LIVE_TEACHER/HANDS_ON/SMALL_GROUP/ONE_TO_ONE/MOVEMENT) are not SELF_PACED/BLEND, so gating DISC_011 behind them would leave that derived fact `UNKNOWN` for the majority of families -- a real regression, not a simplification. The instruction's stated minimum (DISC_011 must show for SELF_PACED/BLEND) is satisfied trivially by leaving it always active. | `src/lib/discovery/stages.ts`, `src/lib/discovery/branching.ts` (EVALUATION_ORDER reorder only -- no predicate change) | RESOLVED |
| DEC-L3 | `preferred_learning_environment` and `desired_primary_change` get a plain `OTHER` card with no inline free-text elaboration, even though both gained an `OTHER` value. | Phase 3F section 2 named exactly five fields (discovery_reasons, reported_support_needs, flexibility_reasons, advancement_interests, family_priorities) for the inline-text mechanism; section 8 explicitly left DISC_012's inline text optional ("if that adds unnecessary complexity, OTHER is optional here; do not add it mechanically") and judged it unnecessary. DISC_008 is a single-choice question outside the named list entirely. | `contracts/question-bank.json` (no `other_text_field` on either) | RESOLVED |
| DEC-L4 | `HONORS_AP` and `CAREER_CTE_CREDENTIALS` are new canonical values with their own new rules (`ADV_014`/`ADV_015`) activating BOTH of their constituent opportunities (OP02+OP03; OP08+OP09) from one selection, rather than the parent choosing between two technical cards. Legacy `HONORS`-only/`AP`-only/`CAREER_CTE`-only/`INDUSTRY_CREDENTIALS`-only answers are **not** aliased to the combined values. | Directly matches the instruction's own guidance: "If one broad Discovery selection must activate more than one possible opportunity category for later review, represent that explicitly in normalization rather than making the parent choose technical distinctions." A historical single-value answer is more precise than the new combined card and should keep activating its own original, more specific rule rather than losing precision by retroactive aliasing. | `contracts/rules.json` (`ADV_014`, `ADV_015`), `contracts/legacy-aliases.json` (deliberately no alias added for these four legacy values) | RESOLVED |
| DEC-L5 | `PHONICS` (reported_support_needs) and `FAMILY_TIME` (flexibility_reasons) are retired from new-entry use via grade-band/tier option lists only -- neither is aliased nor added to `question_value_removals`. | Both instructions explicitly warned against inventing an alias where the meanings are not sufficiently equivalent ("Do not invent an unsafe alias if meanings are not sufficiently equivalent" for FAMILY_TIME; PHONICS is explicitly meant to remain available via DISC_E01, a different field, not disappear from this field's historical effective meaning). A historical answer keeps its own literal effective value going forward. | `contracts/question-bank.json` (`grade_band_allowed_values` only), `contracts/legacy-aliases.json` (no change for either value) | RESOLVED |
| DEC-L6 | Family priorities `ACCREDITATION`/`DIPLOMA`/`ATHLETIC_FLEXIBILITY`/`LOCATION_FLEXIBILITY`/`ADVANCED_COURSES` are retired from new-entry use with no combined replacement value. | The instruction offered a combined "Accredited diploma pathway" concept only "if retaining this information is necessary" and explicitly said not to introduce one unless necessary; none of the five was reachable by any rule, and `scoring-policy.json` already documents that neither ACCREDITATION nor DIPLOMA proves generic-model superiority, so nothing of Phase-4-relevant substance is lost by simple retirement (matching the ACADEMIC_QUALITY precedent from Phase 3E). | `contracts/legacy-aliases.json` (`question_value_removals.family_priorities`) | RESOLVED |

**Owner action:** none required. Phase 4 (the recommendation engine, scoring evaluator, final Discovery Report) remains not authorized and was not started by this section.

## M. Phase 3F.1 — Discovery cleanup patch (RESOLVED, not a Phase 4 start)

Owner-approved follow-up cleanup patch to Phase 3F (section L), addressing three issues found in post-ship manual review: DISC_006 still over the 12-option guideline, "Other" text enforced only at final Submit rather than at Continue, and one stale documentation sentence. Full record at `docs/pathways/DISCOVERY_UX_SIMPLIFICATION_PHASE3F.md` section 10.

| ID | Decision | Reasoning | Implemented at | Status |
|---|---|---|---|---|
| DEC-M1 | `discovery_reasons` (DISC_006) reduced to 11 new-entry choices (10 at ELEMENTARY), superseding DEC-L1's over-12 exception. `ADVANCED_COURSES`/`COLLEGE_ADVANCEMENT` retired-and-aliased to `ACADEMIC_ACCELERATION`; `CREDIT_RECOVERY` retired-and-aliased to `ACADEMIC_SUPPORT`; `SMALLER_ENVIRONMENT` retired-and-aliased to `BETTER_FIT_ENVIRONMENT`. | Now explicitly in scope (the owner named this field directly), and none of these four legacy values lose a real distinction by aliasing: all express a broader parent-facing concept already represented by their surviving value, and none is read by any branch predicate or rule condition on its own literal string (branching.ts reads raw answers directly, unaffected by this effective-answer-only aliasing). | `contracts/question-bank.json` (DISC_006), `contracts/legacy-aliases.json` (`question_value_aliases.discovery_reasons`) | RESOLVED |
| DEC-M2 | `TRAVEL`, `ARTS`, `FAMILY_INVOLVEMENT` are retired from DISC_006's new-entry use with **no** alias and **no** removal -- same pattern as DEC-L5's `PHONICS`/`FAMILY_TIME`. | None of the three has a semantically equivalent surviving DISC_006 value: `TRAVEL`/`ARTS` describe a specific reason for wanting schedule flexibility (now gathered via `flexibility_reasons`' own `FAMILY_TRAVEL`/`ARTS` values), and `FAMILY_INVOLVEMENT` describes a parenting-role preference (now gathered via `desired_parent_involvement`, DISC_027) -- collapsing either into `SCHEDULE_FLEXIBILITY` or another survivor would lose real information rather than merely relabel it. A historical raw answer keeps its own literal effective value. | `contracts/question-bank.json` (`grade_band_allowed_values` only) | RESOLVED |
| DEC-M3 | `schedule_flexibility_need`'s `hasRealConstraint` check gained `ARTS` on `flexibility_reasons` (it already had `FAMILY_TRAVEL`, covering `TRAVEL`'s migration). `supplemental_need`'s `discoveryReasons` check was simplified to drop `ADVANCED_COURSES`/`COLLEGE_ADVANCEMENT` (now unreachable post-normalization). | DEC-M2 moves the ARTS/TRAVEL "real constraint" signal from DISC_006 (always active) to DISC_014 (gated behind rating flexibility SOMEWHAT+) for future submissions; without this compensating check, a genuinely arts-schedule-constrained family who selects ARTS on DISC_014 would silently lose the derived-fact bump Phase 3E's design intended for exactly this signal. The `supplemental_need` simplification is a pure no-op cleanup: those two legacy strings are aliased away by `normalizeMultiValue` before this check ever runs, so checking for them was already dead code. | `src/lib/discovery/normalization.ts` (`deriveScheduleFlexibilityNeed`, `supplementalNeed`) | RESOLVED |
| DEC-M4 | "Other" sidecar text is now required at stage Continue, not only at final Review/Submit, for all five sidecar fields identically on `/discover/demo` and `/discover/profile`, via one shared function (`src/components/discovery/otherTextGate.ts`) rather than a per-component reimplementation. | The owner's stated requirement ("Do not rely only on final Review/Submit validation... use the shared validation/domain architecture rather than duplicating demo-only behavior") is explicit; `validateCompletedProfile`'s existing Review/Submit REQUIRED check (Phase 3F) is kept in place underneath as the server-authoritative backstop for any bypass of the client gate (e.g. a direct `?stage=` navigation). | `src/components/discovery/otherTextGate.ts`, `DiscoveryQuestionnaire.tsx`, `DiscoveryDemoQuestionnaire.tsx`, `QuestionField.tsx` | RESOLVED |
| DEC-M5 | `contracts/question-bank.json`'s live `discovery_reasons` `matching_use` was rewritten to remove "OTHER needs no mandatory narrative"; `docs/pathways/pack/contracts/question-bank.json` (the frozen Phase 0 builder-handoff pack) was deliberately left untouched. | The live contract is what the running application and any future author actually reads, so its inaccuracy needed fixing; the `docs/pathways/pack/` directory is an immutable historical input document (per its own README) that no runtime code loads, so editing it would falsify the historical record rather than correct a live one -- the same "never rewrite historical data" principle already applied to raw answers and legacy-aliases.json. | `contracts/question-bank.json`, `docs/pathways/DISCOVERY_UX_SIMPLIFICATION_PHASE3F.md` (sections 1/6 amended in place, marked superseded) | RESOLVED |

## N. Phase 4 — Deterministic Pathways decision engine (RESOLVED, not merged to main)

Owner-authorized: build the deterministic Discovery decision engine (rule evaluation, derived-fact consumption, educational-alignment scoring, group aggregation, evidence multipliers, candidate qualification/exclusion/consideration, model-family diversity, deterministic ordering, content status, immutable versioned engine-evaluation records, Golden Profiles V2, metamorphic tests, and the contract/schema changes the engine requires). Full spec at `docs/pathways/PHASE4_DECISION_ENGINE_SPEC_V1.md`. Does not build the final Discovery Report, Report UI, AI, provider matching, Blueprint, consultation/advisor/sales workflow, or Phase 5.

| ID | Decision | Reasoning | Implemented at | Status |
|---|---|---|---|---|
| DEC-N1 | `computeEffectiveAnswers` now writes the ALREADY-RESOLVED `primaryReason` (`src/lib/discovery/branching.ts` `computeActiveFlow`/`derivePrimaryReason`) into `effective.answers.primary_discovery_reason`, instead of only ever copying whatever raw value happens to sit behind DISC_007's own `activeFields` membership. | Confirmed by direct inspection: DISC_007 only renders when 2+ discovery_reasons are selected (with exactly one, that reason IS the effective primary reason, no DISC_007 shown), yet `computeEffectiveAnswers` previously destructured only `{ activeFields, gradeBand }` from `computeActiveFlow` and never read its already-computed `primaryReason` at all -- silently discarding the one fact the Phase 4 evidence-multiplier system (section 5 of the spec) depends on for every single-reason profile (5 of the 15 golden personas). A real, pre-existing normalization gap, not a Phase 4-introduced regression; confirmed via the full 240+ pre-existing Vitest suite passing unchanged after the fix. | `src/lib/discovery/normalization.ts` | RESOLVED |
| DEC-N2 | Schedule-flexibility "athlete identity is not itself a schedule conflict" correction: `deriveScheduleFlexibilityNeed` now takes the already-derived `athleticScheduleDemand` and only elevates the tier on a REAL constraint (an actual unavailable time, one of five concrete `flexibility_reasons`, or `athletic_schedule_demand` itself already SUBSTANTIAL/HIGHLY_CONSTRAINED) -- never from being an athlete, a stated `ATHLETIC_TRAINING` preference alone, or the bare `discovery_reasons` ATHLETICS/TRAVEL/ARTS values. `deriveAthleticScheduleDemand`'s own real-academic-conflict check gained `VARIES` (an unpredictable schedule is not less constraining than a fixed one). | Directly required by spec section 7 and proven by metamorphic tests M02/M09 (P06 vs P07: identical athlete identity, only real weekly demand differs, only P06 gets schedule-driven flexible-model evidence). | `src/lib/discovery/normalization.ts`, `tests/discovery-calibration.test.ts` | RESOLVED |
| DEC-N3 | `contracts/scoring-policy.json` rewritten to `2.0.0-phase4-engine`: structured (not prose-only) `baseline`, `score_bounds`, `group_bounds`, `multipliers`, `display_gate` (object), `diversity` (object), `max_displayed_cards`, `tie_break`, `content_status_values`, `desired_change_groups`, `global_review_signals_never_scope_a_candidate`, `state_availability_policy`. `src/lib/contracts/schemas.ts`'s `scoringPolicySchema` updated to match (was flat-string-only). | Section 32 requires one canonical, machine-readable policy source so the engine never parses English policy prose; the prior schema would have thrown a `ContractLoadError` the moment any code loaded the new JSON shape. | `contracts/scoring-policy.json`, `src/lib/contracts/schemas.ts` | RESOLVED |
| DEC-N4 | `contracts/rules.json` -> `2.0.0-phase4-engine`, 77 rules (12 RETIRED). New: `SCHED_HIGH_001`/`SCHED_VERYHIGH_001` (schedule_flexibility_need-driven, replacing `FLEX_001`), `DELIVERY_001B` (ONLINE_TEACHER_SUPPORTED split from `DELIVERY_001`), `SUPPORT_HIGH_001`/`SUPPORT_MODERATE_001`/`SUPPORT_LOW_SELFPACED_001`/`SUPPORT_LOW_SELFPACED_HOMESCHOOL_001`/`SUPPORT_HIGH_ONLINE_TRADEOFF` (support_structure_need-driven), `ACAD_ORG_001`/`ACAD_ENGAGE_001`/`ACAD_SCIENCE_001`/`ACAD_ATTENDANCE_001`/`ACAD_COMMUNICATION_001`, `OPP_002B` (new ARTS trigger on `flexibility_reasons`, `OPP_002` kept for legacy explainability), `COST_ALIGNMENT_REVIEW` (feasibility-only, empty score_effects). Retired: `FLEX_001`, `PAR_001` (blanket PROGRAM_MANAGES reward, no replacement), `IND_002`, `CONF_001`, `ACAD_007`, `ATH_002`, `CURR_001`/`CURR_002` (-> engine-native B01 continuity function), `LEARN_001` (B02/B05/B07 raw-preference support_structure evidence, confirmed redundant with the new derived-fact rules). `LEARN_002` kept but narrowed to its B03/B06 effect only (its own B07 effect removed) -- the sole positive support_structure-group evidence ever available to the virtual family under the literal section-15 tables, confirmed necessary for P03 and confirmed to over-qualify B07 for P06 if kept. `PAR_002`/`HOME_002` moved onto the `family_management_preference` derived fact; `PAR_002` also activates the new `REV_PROGRAM_MANAGEMENT`; `ELEM_001`'s `REV_ADULT_SUPPORT` scope widened to include B07; `FLEX_002`/`FLEX_003`/`IND_001` gained a missing `review_model_scope` (they activated a candidate-scoped review signal globally before this fix); `FLEX_003` no longer activates `REV_STATE_AVAILABILITY` directly (now engine-native only, avoiding a double source with a narrower rule-level scope that would have wrongly excluded B07 from `REV_SCHEDULE_COMPATIBILITY` too). | Sections 12-27's exact per-topic rule specifications, implemented as literally as possible; the `LEARN_001`/`LEARN_002` and review-scope fixes were discovered only by running the actual owner-calibrated golden fixtures against the engine (not asserted) -- see DEC-N7 for the three that remained unresolved after this pass. | `contracts/rules.json` | RESOLVED |
| DEC-N5 | `contracts/taxonomy.json` -> `2.0.0-phase4-engine`: three new `review_signals` (`REV_PROGRAM_MANAGEMENT`, `REV_ATTENDANCE_CONTEXT`, `REV_COMMUNICATION_CONTEXT`), all global (never candidate-scoped). `src/lib/contracts/validate.ts` extended: FEASIBILITY rules must have empty `score_effects`; `cost_preference`/`desired_start_timeline`/`parent_context`/`primary_sport`/`athletic_level`/every `*_other_text` field can never appear in a rule condition alongside a non-empty `score_effects`. | Sections 19/27/33. Negative-control tests added to `tests/contracts.test.ts` proving each new check actually fires, not merely that the current contracts happen to pass it. | `contracts/taxonomy.json`, `src/lib/contracts/validate.ts`, `tests/contracts.test.ts` | RESOLVED |
| DEC-N6 | New `src/lib/engine/` module (`types.ts`, `conditions.ts`, `rules.ts`, `scoring.ts`, `candidates.ts`, `considerations.ts`, `evaluate.ts`, `hash.ts`, `index.ts`). `evaluateDiscoveryProfile(effective, contracts)` is a pure function producing an immutable `EngineEvaluation`; `createEngineRun(evaluation, metadata)` wraps it with an injectable clock/run ID. No database read or write anywhere in this module. | Section 31/34. Pure deterministic business logic with no UI, no DB, no dynamic eval, typed `eq`/`in`/`contains_any` conditions only, B10 excluded at the candidate-universe level (not merely by score), stably sorted output, no dependence on JSON rule array order (M07) or object-insertion order. | `src/lib/engine/*` | RESOLVED |
| DEC-N7 | Three assertions across two personas (P04's B07, P14's B04, P12's B03-vs-B06 representative) did not pass under a strictly faithful, literal implementation of sections 8-27, and were not fixed at the time by weakening the golden fixture or adding persona-keyed code. An interim commit (`41c0d63`) marked exactly those three assertions `it.fails` as a stopgap so CI reflected "known, tracked, unresolved" rather than an opaque red. **Superseded and resolved in Phase 4.1** (DEC-N8/DEC-N9 below): the `it.fails` mechanism, and every `knownConflicts`/`knownConflictNote` fixture annotation, are removed entirely. All three now pass as ordinary must-pass tests. | Full root-cause tracing (real engine runs, not assumption) identified two general engine/rule gaps -- see DEC-N8 and DEC-N9. | `fixtures/golden-profiles.json`, `tests/engine-golden.test.ts` | RESOLVED (see DEC-N8, DEC-N9) |

**Owner action:** none required for this section as of Phase 4.1 -- DEC-N7 is resolved. Phase 5, the final Discovery Report, Report UI, AI and provider matching remain not authorized and were not started.

## O. Phase 4.1 — Resolve owner-calibrated engine acceptance conflicts (RESOLVED, not merged to main)

Owner-authorized narrow correction: resolve DEC-N7 so all 15 golden profiles pass as ordinary must-pass tests, with no `it.fails`, skip, or known-conflict annotation anywhere. No Discovery Report, no AI, no questionnaire changes, no persona-ID logic, no Phase 5. Full spec at `docs/pathways/PHASE4_DECISION_ENGINE_SPEC_V1.md` sections 6a and 11.

| ID | Decision | Reasoning | Implemented at | Status |
|---|---|---|---|---|
| DEC-N8 | New **Directional Evidence Gate**, applied after the existing display gate, for B02-B09 only (B01 keeps its own STAY_CURRENT exception untouched). A candidate qualifies only when it ALSO has either (A) a raw (pre-multiplier) positive contribution `>= 2` in `delivery`, `family_role`, or `continuity`, or (B) at least 3 distinct scoring groups with a final positive contribution. Implemented in `src/lib/engine/candidates.ts` (`evaluateDirectionalEvidenceGate`), reading only the group-contribution provenance `scoring.ts` already computes -- never final public labels, never a persona or profile identity. | `schedule_flexibility_need`/`support_structure_need` are family-level, not model-specific, needs; either alone can satisfy the base gate's "2 groups, 1 linked" test without pointing at a *specific* candidate. This let P04's B07 (schedule + support_structure only, no direct model evidence) and P14's B04 (schedule + a weak +1 delivery signal) both mechanically qualify. With the gate: B07 fails for P04 (no strong direct evidence, only 2 generic groups); B04 fails for P14 (same reason); B08/B09 (P04) and B03/B06/B07 (P14) are unaffected, each already clearing the gate via strong/broad evidence of their own. P02/P06's B04 (three independent positive groups: schedule+delivery+support_structure) continues to qualify exactly as those fixtures require -- confirmed by re-running the full golden suite, not merely reasoned about. | `src/lib/engine/candidates.ts`, `src/lib/engine/evaluate.ts`, `src/lib/engine/index.ts` | RESOLVED |
| DEC-N9 | `FLEX_002`'s B06 (private online) score dropped from `+3` to `+2`, now identical to B03 (public virtual). B04/B07/B08/B09 unchanged. | Unavailable academic times are generic remote/virtual-schedule evidence; they never established that a *privately funded* online model is inherently more schedule-compatible than a *publicly funded* virtual one, and public/private funding model is not itself educational schedule evidence -- a real scoring bug, not a documentation gap. This was the sole remaining source of a non-tied B03-vs-B06 score for P12, which needs them genuinely tied so the existing (never cost-driven) stable-ID tie-break picks B03 as required. | `contracts/rules.json` (`FLEX_002`) | RESOLVED |
| DEC-N10 | Removed the Phase 4 follow-up `it.fails`/`knownConflicts`/`knownConflictNote` mechanism entirely from `tests/engine-golden.test.ts` and `fixtures/golden-profiles.json`. No P01-P15 expected value was changed. | Once DEC-N8/DEC-N9 made all three assertions pass under the engine's real, generalized behavior, the stopgap escape hatch from DEC-N7 no longer had anything to mark -- keeping it would have hidden a now-passing suite behind unnecessary annotation. | `tests/engine-golden.test.ts`, `fixtures/golden-profiles.json` | RESOLVED |
| DEC-N11 | Added `tests/engine-directional-gate.test.ts`: 12 regression tests proving (A) two generic groups alone never qualify a candidate without strong direct evidence, (B) three independent positive groups qualify a candidate even with only weak direct delivery evidence, (C) FLEX_002 scores B03/B06 identically (both at the contract level and via P14's actual internal scores), (D) cost preference (all 4 enum values) never changes P14's B03/B06/B07 educational scoring, qualifying or displayed candidates, and (E) the gate's verdict depends only on contribution-provenance shape, not on which candidate model ID or persona it is attached to. | Section 5 of the Phase 4.1 instruction. | `tests/engine-directional-gate.test.ts` | RESOLVED |

**Owner action:** none required. All 15 golden profiles (P01-P15) pass as ordinary required tests; no Golden Profile is marked expected-fail. Phase 5, the final Discovery Report, Report UI, and AI remain not authorized and were not started by this phase.

## P. Phase 5 — Deterministic personalized Discovery Report + report UI (IMPLEMENTED, not merged to main)

Owner-authorized: build the Phase 5 deterministic personalized Discovery Report layer and premium report UI on top of the unmodified Phase 4.1 engine -- report domain module, report content contract, report UI components, the production report route, a DB-free synthetic report demo route, seven owner-calibrated Golden Report fixtures, and the full structural/invariant/forbidden-copy test suite. Full spec at `docs/pathways/PHASE5_DISCOVERY_REPORT_SPEC_V1.md`. Does not touch Discovery questions/wording, Phase 4 scoring/qualification/ordering/public labels, AI, provider matching, Blueprint, enrollment/advisor/payment workflow, or sales/lead scoring.

| ID | Decision | Reasoning | Implemented at | Status |
|---|---|---|---|---|
| DEC-P1 | New `src/lib/report/` domain module (`types.ts`, `archetypes.ts`, `snapshot.ts`, `insights.ts`, `directions.ts`, `support-map.ts`, `comparisons.ts`, `pathway.ts`, `cta.ts`, `provenance.ts`, `assemble.ts`, `index.ts`). `assembleDiscoveryReport(input, contracts, createdAt)` is a pure function reading an already-final `EngineEvaluation` and never rescoring, reranking, replacing, or adding a candidate. | Section 4/34/35: the absolute Phase 4/Phase 5 boundary. Enforced by RPT-M01/RPT-M02/RPT-M08 (report-content reordering, a qualifying-but-non-displayed candidate, and JSON key reordering can never change a displayed candidate or its order). | `src/lib/report/*` | RESOLVED |
| DEC-P2 | New `contracts/report-content.json` (`1.0.0-phase5-report`): archetype copy, per-model candidate-card copy (archetype-specific with a generic fallback), support/opportunity/overlay tile copy, review-signal public translations, pathway/CTA templates, the one authorized `PROFILE_CONTEXT:cost_preference` comparison question, and `forbidden_claims`. `contracts/report-contract.json` bumped to `2.0.0-phase5-report` to describe the now-real DTO shape; `contracts/content-library.json` untouched. | Section 3/8/16/22/32: one canonical, versioned, content-only registry, kept fully separate from Phase 4's educational-decision contracts. | `contracts/report-content.json`, `contracts/report-contract.json` | RESOLVED |
| DEC-P3 | `src/lib/contracts/schemas.ts`/`loader.ts`/`validate.ts` extended: `reportContentSchema` + `loadReportContent`, and a new `validateReportContent` check folded into `validateContracts`'s single result -- every ACTIVE base model/support/opportunity/review-signal has approved content or an explicit non-ACTIVE exclusion, no `B10` public content, no RESERVED/RETIRED opportunity ever public, every archetype complete, no forbidden-claim string in the approved copy. | Section 33/65. | `src/lib/contracts/schemas.ts`, `loader.ts`, `validate.ts`, `index.ts` | RESOLVED |
| DEC-P4 | Deterministic, persona-never-keyed report-archetype selection (`selectReportArchetype`), 8 archetypes, fixed precedence order reading only `contentStatus`/`derivedFacts`/activated ids/`displayedCandidateIds`/`costPreference`. | Section 9/64 (RPT-M10). Verified against all 15 golden personas' real engine output; the 7 owner-narrated fixtures each resolve to their required archetype on the first matching precedence rule. | `src/lib/report/archetypes.ts` | RESOLVED |
| DEC-P5 | New read-only `loadLatestCompletedRevision(db, sessionId)` in `src/server/discovery-draft.ts`, alongside (never replacing) the existing `hasCompletedRevision`/`reopenForEditing`. `app/discover/report/page.tsx` rewritten: real completed-profile load -> `validateCompletedProfile` -> `evaluateDiscoveryProfile` -> `assembleDiscoveryReport` -> `<ReportView>`, keeping the DEC-G10 dynamic-import discipline and the existing incomplete-profile screen and `reopenForEditingAction` edit flow. No new report database table -- V1 assembles deterministically on every request from the already-stored `rawAnswers`/`createdAt`. | Section 36/37/38: "load completed profile revision -> compute effective answers -> run Phase 4 engine -> assemble Phase 5 ReportDTO -> render", no second competing data-access path, no new persistence unless already required (the pre-existing `reportSnapshot`/`engineRun` tables stay unused -- nothing in this phase writes to them), no email wall. | `src/server/discovery-draft.ts`, `app/discover/report/page.tsx` | RESOLVED |
| DEC-P6 | New DB-free `app/discover/report/demo/page.tsx`: only the 7 named `?fixture=GR0x` ids are ever accepted (anything else falls back to `GR01`), no database import anywhere in its module graph, reads `fixtures/golden-profiles.json` raw answers directly and runs the identical live assembler/components as production. | Section 39/66: exists so the report can be visually reviewed without DEC-G9's DB gap; `fixtures/golden-reports.json` stays the expectation source, never a pre-rendered snapshot this route reads. | `app/discover/report/demo/page.tsx` | RESOLVED |
| DEC-P7 | New `src/components/report/*` (ReportHero, PriorityChips, InsightSection, DirectionCard/DirectionGrid, SupportOpportunityMap, ComparisonGuide, PathwayRoadmap, ConversionBand, MobileReportCta, ReportView). Equal-weight model cards (no rank/"Top Match"), abstract-motif hero (no stock photo), accessible parallel-branch pathway stepper, mobile sticky CTA appearing only after R03 via IntersectionObserver. | Section 15/25/31/40-48. Verified overflow-free at 375/768/1440px for all 7 golden fixtures via Playwright; a genuine mobile-overflow bug (an un-shrinkable fit-label badge) was found and fixed during this verification, not merely asserted absent. | `src/components/report/*` | RESOLVED |
| DEC-P8 | New `fixtures/golden-reports.json` (`1.0.0-owner-calibrated`) referencing `personaId` into `fixtures/golden-profiles.json`. New test files: `tests/report-golden.test.ts` (94 tests, real pipeline, GR01/GR03/GR06/GR09/GR12/GR14/GR15 exact + concept assertions), `tests/report-structural.test.ts` (150 tests, all 15 personas), `tests/report-invariants.test.ts` (13 tests, RPT-M01-M10), `tests/report-forbidden-copy.test.ts` (15 tests). | Section 54-65. Full Vitest suite: 421 pre-existing Phase 4.1 tests + 272 new Phase 5 tests = 693 passed, 0 regressed. | `fixtures/golden-reports.json`, `tests/report-*.test.ts` | RESOLVED |
| DEC-P9 | Two documented, non-blocking deviations from the instruction's literal R04 narrative: P14 additionally shows a genuinely-activated `S08` (College Planning) support tile the instruction's own narrative did not list (Phase 4's real `college_intent = PROBABLY` activation, never suppressed to force a narrative match); priority-chip label wording uses one general label table rather than per-fixture text (chips are outside section 62's exact-match list). | Phase 5 must render what Phase 4 actually activates, never suppress real activation to match a stale narrative assumption, and must never key content to a persona/fixture id. Neither deviation touches any of section 62's actual exact-match fields. | `docs/pathways/PHASE5_DISCOVERY_REPORT_SPEC_V1.md` section 14 | RESOLVED (documented, not a defect) |

**Owner action:** none required. GR01/GR03/GR06/GR09/GR12/GR14/GR15 all pass their owner-calibrated acceptance tests; no non-displayed Phase 4 candidate is ever surfaced; no public score/percentage is shown; AI, provider matching, enrollment/advisor/payment workflow and sales/lead scoring were not added. Phase 6 and beyond remain not authorized and were not started by this phase.

## Q. Phase 5.1 — End-to-end interactive Discovery demo integration (IMPLEMENTED, not merged to main)

Owner-authorized integration phase: connect the existing DB-free interactive Discovery questionnaire demo (`/discover/demo`) to the real, unmodified Phase 4 engine and the real, unmodified Phase 5 report assembler, so it generates the visitor's own personalized Discovery Report from their current in-memory answers. Full record at `docs/pathways/PHASE5_1_END_TO_END_DEMO_INTEGRATION.md`. Does not touch Phase 4 scoring/rules/candidate qualification/ordering/public labels, Phase 5 report content/wording, Discovery question wording/branching, AI, provider matching, or any database/session/cookie/browser-storage/URL persistence for demo state.

| ID | Decision | Reasoning | Implemented at | Status |
|---|---|---|---|---|
| DEC-Q1 | New `src/lib/report/profile-context.ts` exports `buildReportProfileContext({ rawAnswers, profileRevisionId, gradeBand })`, replacing three previously-separate inline copies of the same presentation-only field-extraction logic (production report route, Golden fixture demo route, and the new interactive demo action). | One canonical extraction path for the same non-decision-making data shaping; confirmed byte-identical output for all three callers via the new parity tests (DEC-Q5). | `src/lib/report/profile-context.ts`, `src/lib/report/index.ts` | RESOLVED |
| DEC-Q2 | New DB-free `buildDemoDiscoveryReport(rawAnswers)` server action in `app/discover/demo/actions.ts`: validates the full raw answer snapshot, runs the real Phase 4 engine, derives a deterministic (never random) `profileRevisionId` from `evaluation.effectiveProfileHash`, and assembles the report with a fixed `consultationState: "UNCONFIGURED"`/`saveAvailable: false` and a fixed demo `createdAt`. No database, session, cookie, file write, or server-side answer cache anywhere in the path. | The exact "recompute from the entire current in-memory snapshot on every call" stateless server-action pattern already established for the demo's DB-free questionnaire recompute layer (Phase 3C), extended to report generation rather than only question-state recomputation. | `app/discover/demo/actions.ts` | RESOLVED |
| DEC-Q3 | `ReportView`'s `isDemoRoute: boolean` prop replaced by `demoLabel?: string` (presentation-only notice text) plus a new `secondaryTopAction?: ReactNode`. This is a UI component prop-API change, not a report-contract change — `DiscoveryReportDTO`/`contracts/report-contract.json` are unchanged. | Needed one generalized label so the interactive demo ("Interactive Discovery demo — answers are not saved") and the Golden fixture demo ("Synthetic report demo") could each supply their own accurate, distinct notice through the same shared component, and so the interactive demo could add its own "Start Demo Again" action alongside the existing edit-answers action. | `src/components/report/ReportView.tsx`, `.module.css`, `app/discover/report/demo/page.tsx` | RESOLVED |
| DEC-Q4 | `DiscoveryDemoQuestionnaire.tsx`'s old `CompletionScreen` ("Discovery Demo Complete") removed entirely. Report generation happens in place on the Review stage (`stageId` never changes across generating/editing/erroring); `report: DiscoveryReportDTO \| null` plus an `isBuildingReport` flag and an optional `reportError` message live in React state only. Both flows' Review button copy changed from "Complete My Discovery Profile"/"Submitting…" to "See My Personalized Discovery Report"/"Building Your Discovery Report…". | The old placeholder completion screen was a dead end that never showed the actual product; keeping both it and the new report path active would leave two divergent "done" states. The button copy no longer promises a "Submit" that doesn't happen (nothing is submitted anywhere — it's a stateless recompute) and now accurately describes report generation. | `src/components/discovery/DiscoveryDemoQuestionnaire.tsx`, `src/components/discovery/DiscoveryQuestionnaire.tsx`, `app/discover/demo/page.tsx` | RESOLVED |
| DEC-Q5 | New `tests/discovery-demo-report.test.ts`: pipeline tests for P01/P03/P09/P12/P15 against a directly-computed `evaluateDiscoveryProfile` result (no mocked `EngineEvaluation`), plus demo/Golden-fixture parity tests for GR01/GR03/GR12/GR15 asserting the interactive builder's `DiscoveryReportDTO` matches the Golden-fixture pipeline's exactly, except for envelope/demo metadata (`profileRevisionId`/`reportId`/`createdAt`). | Proves the interactive demo's report path is architecturally identical to, not a reimplementation of, the canonical Phase 4/Phase 5 pipeline. All 25 new tests passed on first run. | `tests/discovery-demo-report.test.ts` | RESOLVED |
| DEC-Q6 | New `tests/e2e/discovery-demo-integration.spec.ts`, driving the questionnaire through the UI at `/discover/demo` (never the fixture route): questionnaire → real report (hero/insight/directions/pathway/conversion sections, old completion screen absent, Golden fixture selector absent, demo label present), keyboard-only operation of the final Review CTA, no horizontal overflow at 375px, edit-and-regenerate proving live-answer-driven content (changing the student's display name and confirming the regenerated report reflects it), Start Demo Again clearing all state, a browser refresh restarting the demo after a report was generated, and no client-side answer/report persistence (storage or cookies) after generating a report. Four pre-existing Playwright specs (`discovery-demo.spec.ts`, `discovery-profile.spec.ts`, `discovery-scroll.spec.ts`, `discovery-visual-qa.spec.ts`) were updated for the new button wording/report-based completion state; `report-demo.spec.ts` (Golden fixture demo) was confirmed unaffected. | Section 22-26/34 of the Phase 5.1 instruction. All 160 Playwright tests in the suite pass. | `tests/e2e/discovery-demo-integration.spec.ts` and the four updated spec files above | RESOLVED |
| DEC-Q7 | Both demo routes' pages (`app/discover/demo/page.tsx`, `app/discover/report/demo/page.tsx`) carried their own pre-existing visually-hidden landmark `<h1>` for their `<Section>`'s `aria-labelledby`, which sat alongside the report's own `<h1>` once a report rendered on `/discover/demo` too — the same duplication the Golden fixture demo route already had before Phase 5.1. Not changed in Phase 5.1 itself (Playwright assertions there scoped past the known static hidden id, `h1:not(#discovery-demo-heading)`, rather than assuming a single unscoped `<h1>`). **Resolved in Phase 5.1a** — see section R below. | A real, pre-existing minor semantic duplication, not introduced net-new by Phase 5.1's integration (the interactive demo reused the identical hidden-landmark-heading pattern the Golden fixture route already used). | `docs/pathways/PHASE5_1_END_TO_END_DEMO_INTEGRATION.md` section 30 (superseded by section R) | RESOLVED (Phase 5.1a) |

**Owner action:** none required — DEC-Q7 is resolved (see section R). Phase 4 educational decisions and Phase 5 report content were not changed by this phase, no AI or persistence was added, and no merge to main occurred.

## R. Phase 5.1a — Final end-to-end demo acceptance cleanup (RESOLVED, not merged to main)

Owner-authorized narrow acceptance cleanup on top of Phase 5.1: (1) strengthen the edit → regenerate Playwright test to change a material, Phase-4-relevant structured Discovery answer rather than only a cosmetic display name, and (2) resolve DEC-Q7 (one H1 per rendered experience) without any visual redesign. Full record at `docs/pathways/PHASE5_1_END_TO_END_DEMO_INTEGRATION.md` addendum. Does not touch Phase 4 scoring/rules/candidate qualification/ordering/public labels, Phase 5 report content/wording, Discovery question wording/branching, AI, persistence, or database/session behavior.

| ID | Decision | Reasoning | Implemented at | Status |
|---|---|---|---|---|
| DEC-R1 | New Playwright test changes `school_change_preference` (DISC_031, "Which best describes what you're hoping for right now?") from unanswered/`UNKNOWN` to `STAY_CURRENT` ("Improve what we already have") between the initial report and the regenerated one, on the same stable K-4 input path Phase 5.1 already established. This is a real Phase 4 engine input (the engine-native B01 continuity qualification, `src/lib/engine/candidates.ts`) — never a cosmetic/display-only field. | Directly verified via the real pipeline (no mocked engine) that this single-field edit changes `contentStatus` (`LIMITED_INFORMATION` → `PERSONALIZED`), `displayedCandidateIds` (`[]` → `["B01"]`), the hero headline, the insight headline, and the pathway title — a clearly educational Phase 4/Phase 5 output change, satisfying the "material structured answer" requirement no cosmetic edit could. The existing student-name-change test (DEC-Q6) is kept alongside it, not replaced, since it still usefully proves the envelope-level "no cached/fixture result" guarantee. | `tests/e2e/discovery-demo-integration.spec.ts` | RESOLVED |
| DEC-R2 | DEC-Q7 resolved: `app/discover/demo/page.tsx`'s and `app/discover/report/demo/page.tsx`'s route-landmark heading, used only as an `aria-labelledby` target for the surrounding `<Section>`, is now a non-heading `<span id="..." className="visually-hidden">` instead of an `<h1>` (`aria-labelledby` only needs an element with an accessible name, not a heading). `DiscoveryDemoQuestionnaire.tsx` now renders its own visually-hidden `<h1>Discovery Preview Demo</h1>` directly, but only while no report exists (questionnaire/Review/building/error states); the moment a report exists, that H1 stops rendering and the report's own `ReportHero` `<h1>` becomes the page's sole H1. `/discover/report/demo` always renders a report, so its landmark `<span>` change alone was sufficient there. | Exactly one H1 per rendered experience is now true structurally (verified in real Chromium, not merely reasoned about) rather than requiring a test-side `:not(#id)` workaround: questionnaire state, interactive report state, and the Golden fixture demo state each resolve to exactly one `<h1>`. No visual change -- both the removed and the added heading were/are `visually-hidden`; only the semantic tag moved. | `app/discover/demo/page.tsx`, `app/discover/report/demo/page.tsx`, `src/components/discovery/DiscoveryDemoQuestionnaire.tsx` | RESOLVED |
| DEC-R3 | New Playwright assertions replace every `h1:not(#discovery-demo-heading)` workaround with a plain `page.locator("h1")` count-1 assertion, added across the questionnaire state, the interactive report state, and the Golden fixture demo route. | DEC-R2 makes the workaround unnecessary; keeping it would have hidden a real regression (a future second H1) behind a selector that silently excludes one candidate element. | `tests/e2e/discovery-demo-integration.spec.ts`, `tests/e2e/discovery-demo.spec.ts`, `tests/e2e/discovery-scroll.spec.ts` | RESOLVED |
| DEC-R4 | `discovery-scroll.spec.ts`'s "the generated report and Start Demo Again both scroll to the beginning of their content" test asserted the ReportHero `<h1>` itself landed near the viewport top after the scroll-into-view effect fired. Reproduced with the pre-Phase-5.1a markup too (confirmed by temporarily reverting DEC-R2's changes and re-running): the H1 sits well below the demo label, top actions, and ReportHero's own eyebrow/title/badge, so it was never reliably near the top even when the scroll behavior worked correctly -- a pre-existing, environment-timing-dependent test assertion, not a product regression. Fixed by checking the demo label (the anchor container's actual first visible content) instead. | A test bug, not an app bug: `useScrollStageAnchor` scrolls the report's outer container into view, and the container's own first content is the correct proxy for "scrolled to the beginning," never the H1 buried inside it. Verified passing 3/3 consecutive runs after the fix, versus a deterministic failure 3/3 times before it (in this environment). | `tests/e2e/discovery-scroll.spec.ts` | RESOLVED |

**Owner action:** none required. Phase 4 educational decisions and Phase 5 report content were not changed by this phase; no AI or persistence was added; no merge to main occurred.

## S. Phase 5.2 — Discovery Report visual & conversion polish (RESOLVED, not merged to main)

Owner-authorized presentation-layer redesign of the Discovery Report toward a "premium personalized education advisory product" feel, per `docs/pathways/PHASE5_2_VISUAL_CONVERSION_POLISH.md`. Server Components + CSS Modules only; no new client component or state (`MobileReportCta` remains the sole client component). Phase 4 engine (`contracts/rules.json`/`scoring-policy.json`/`taxonomy.json`/`question-bank.json`, `src/lib/engine/**`, `fixtures/golden-profiles.json`) and Phase 5 report content (`contracts/report-content.json`, `fixtures/golden-reports.json`) are byte-unchanged; no contract version was bumped. `DiscoveryReportDTO` is unchanged.

| ID | Decision | Reasoning | Implemented at | Status |
|---|---|---|---|---|
| DEC-S1 | New `ReportUtilityHeader` component replaces the previous bare `demoLabel`/`topActions` strip: left side reuses the existing `Logo` wordmark plus a "Discovery Report" label and the optional demo badge; right side keeps the same edit-answers action/override and secondary action (e.g. "Start Demo Again") unchanged. | Section 22 — no new logo asset, reuse the existing wordmark, presentational only. | `src/components/report/ReportUtilityHeader.tsx`, `.module.css`, `ReportView.tsx` | RESOLVED |
| DEC-S2 | `DirectionCard`'s decorative motif no longer renders `card.baseModelId` text at all (previously visible inside an `aria-hidden` span, which does not hide it from sighted users) — replaced with a purely decorative inline SVG glyph. "Why this surfaced" and "What to look for" are now two separate DOM regions (`evidenceZone`/`actionZone`) with distinct headings ("Why This Surfaced" / "What To Look For"), distinct icons, and distinct green/blue-navy tint + left-border treatment — never sharing typography or background. | Sections 5/6/28/37 — the previous shared-typography treatment and the visible internal ID were exactly what the owner's Phase 5.2 review flagged. `card.baseModelId` itself is untouched in the DTO. | `src/components/report/DirectionCard.tsx`, `.module.css` | RESOLVED |
| DEC-S3 | R03's section heading ("Directions Worth Exploring") is now a visible `<h2>` for personalized reports (previously `visually-hidden`). The zero-card special state now has two distinct presentational wrappers driven by `report.contentStatus` (passed down as a new prop, DTO itself unchanged): ADVISOR_FIRST renders a confident navy/blue "under review" treatment with a shield-check icon; LIMITED_INFORMATION renders a warmer/lighter "still exploring" treatment with a compass icon. Both reuse the exact same `emptyStateHeading`/`emptyStateBody` DTO content — no new content field. | Sections 7/11 — never a rank/"Top Match" implication; never presenting a deliberate zero-recommendation state as if it were the same visual "error" as a truly missing recommendation. | `src/components/report/DirectionGrid.tsx`, new `DirectionGrid.module.css` | RESOLVED |
| DEC-S4 | `SupportOpportunityMap`'s tiles are now smaller, icon-led (a distinct linked-circles glyph for SUPPORT vs. an upward-arrow glyph for OPPORTUNITY), on a warm-neutral (`--color-bg-alt`) surface with a 1px border and no card shadow — deliberately lighter than R03's cards. A new "The ecosystem around this direction" eyebrow frames the section. The ADVISOR_FIRST/LIMITED_INFORMATION special-state tiles reuse the same `specialHeading`/`specialTiles` DTO content inside a similarly differentiated wrapper (navy-accent vs. warm-neutral). | Section 9/10 — must read as "the ecosystem", not a second row of recommendations; SUPPORT vs. OPPORTUNITY distinguished by icon+color+typography together, never color alone. | `src/components/report/SupportOpportunityMap.tsx`, `.module.css` | RESOLVED |
| DEC-S5 | `ComparisonGuide` (R05) keeps the existing `guide.title` (report-content.json-sourced) but demotes it to a small eyebrow; a new UI-only `<h2>` "Questions That Matter Before You Choose" and supporting copy "These are the questions worth answering before you choose a direction or program." were added directly in the component (never in `report-content.json`). Numbered markers are larger (40px, solid navy), question typography is stronger, and per-question separators are a subtle top border rather than a repeated white card. | Section 12/13 — explicitly authorized as UI-layer-only text; the underlying `questions`/`explanations` are still exactly the DTO's. | `src/components/report/ComparisonGuide.tsx`, `.module.css` | RESOLVED |
| DEC-S6 | `PathwayRoadmap` (R06) is now a single continuous vertical stepper at every viewport (the previous ≥900px switch to a CSS-grid auto-fit column layout — the actual cause of GR12 reading as flat/sequential — was removed entirely). A connector line threads behind every numbered stage node. The parallel-group step (GR12) now visibly forks off that line into a bracketed two-track box (`role="group" aria-label="Parallel pathway priorities"`, with the same label rendered visibly) and then continues the single line to the next stage — split → parallel tracks → rejoin, readable without the subordinate paragraph. Genuinely sequential pathways (GR09/GR15/GR03) are unaffected structurally, only more premium in spacing/typography. | Sections 14-17/39/47 — `groupStages()`'s existing accessible one-`<li>`-per-parallel-group grouping was already correct; the bug was purely visual/CSS, and the missing accessible group label (section 15) is now present both visibly and to assistive tech. | `src/components/report/PathwayRoadmap.tsx`, `.module.css` | RESOLVED |
| DEC-S7 | `ConversionBandFull`/`InlineConversionBand` (R07) gained a subtle arrow icon, larger CTA padding/typography, a full-width mobile CTA, and a presentational-only `contentStatus` accent variant (a top accent bar + CTA color: green for PERSONALIZED, blue for ADVISOR_FIRST, a muted neutral for LIMITED_INFORMATION) — `action.label`/`action.href` are passed through completely unchanged from `report.actions.primary` in every case, so CTA-resolution safety (`src/lib/report/cta.ts`) is untouched. The inline band placed after R03 now uses contentStatus-specific copy (new UI-layer strings in `ReportView.tsx`, e.g. "Ready to have this reviewed with Pathways?" for ADVISOR_FIRST, "Want help figuring out what to look for next?" for LIMITED_INFORMATION) instead of the previous hardcoded PERSONALIZED-only copy, still rendering the same `report.actions.primary`. | Sections 18-21 — never touches CTA-resolution logic; UNCONFIGURED still always resolves to "See What Comes Next"/the safe informational href, verified by new Playwright coverage across 5 fixtures. | `src/components/report/ConversionBand.tsx`, `.module.css`, `ReportView.tsx` | RESOLVED |
| DEC-S8 | `app/discover/report/demo/page.tsx`'s fixture-selector nav is now `flex-wrap: nowrap` with `overflow-x: auto`, so on narrow viewports it scrolls horizontally as a single row instead of wrapping onto multiple lines — no new client JS, plain CSS on the existing server-rendered `<a>` list. | Section 30 — compact on mobile without a second rendering path or client component. | `app/discover/report/demo/page.tsx` | RESOLVED |
| DEC-S9 | New `tests/e2e/report-visual-polish.spec.ts` (24 tests): no visible `B0N` candidate ID text across 5 fixtures; evidence/action zones render as two distinctly-headed regions; the R03 heading is visible; GR03's two cards are equal width; the R04 ecosystem eyebrow renders; R05's new heading/subcopy render alongside the original `guide.title`; GR12's parallel group is one accessible `role="group"` containing both branch labels, renders side-by-side at 1440px and stacked-but-grouped at 375px with no horizontal overflow; CTA safety across 5 fixtures (always "See What Comes Next", never "Build My Student's Pathway", since the demo route is always `UNCONFIGURED`); the 3 contentStatus-specific inline-band copy variants; GR09/GR15 never use "no results"/"error" language; the new utility header renders identity + demo label + edit-answers link. All 24 pass, plus the full pre-existing suite (185 Playwright tests, 781 Vitest tests including the DB-backed suites) re-verified with zero regressions. | Sections 35-44/54 — new structural guarantees this phase adds, without weakening any existing assertion. | `tests/e2e/report-visual-polish.spec.ts` | RESOLVED |
| DEC-S10 | Re-confirmed DEC-G4 (the sandbox-local `pnpm build` `/_global-error` prerender anomaly) independently in this phase via `git stash -u` + rebuild on the unmodified Phase 5.1a `HEAD`: identical failure reproduced with zero Phase 5.2 changes present, before any Phase 5.2 file was touched. Not fixed (out of Phase 5.2 scope; already diagnosed in DEC-G4 as a sandbox/Turbopack-vs-container anomaly that does not reproduce on GitHub Actions' `ubuntu-latest` runner). | Full validation (section 55) must report every result accurately, including a pre-existing, already-documented, non-blocking local anomaly, without treating it as new or hiding it. | (no change — pre-existing, see DEC-G4) | CONFIRMED PRE-EXISTING, NOT BLOCKING |

**Owner action:** none required. Phase 4 educational decisions, Phase 5 report content (`contracts/report-content.json`, `fixtures/golden-reports.json`), and the `DiscoveryReportDTO` shape were not changed by this phase; no AI, provider matching, booking, payment, lead scoring, new UI framework, icon library, or font package was added; no merge to main occurred.

| DEC-S11 | Phase 5.2a visual acceptance pass: manual screenshot QA (local dev server, 375/768/1440px) across the organic report and GR03/GR12/GR09/GR15 found R06's numbered stage node absolutely positioned without reserved flex-gap space, so every stage label rendered overlapping its own number at every breakpoint/fixture -- blocking visual review of R06 entirely. Fixed in commit `1cc7e57` (node reverted to a normal flex item; connector spine unchanged). No other code changed in that pass; the ADVISOR_FIRST/LIMITED_INFORMATION inline-copy deviation and the sequential-vs-parallel R06 layout question were flagged to the owner rather than changed unilaterally. | Section 10 of that review instruction explicitly permitted fixing "an obvious rendering bug preventing visual review"; the copy/layout questions were product decisions requiring owner input, not blockers to review itself. | `src/components/report/PathwayRoadmap.module.css` | RESOLVED |
| DEC-S12 | Owner-approved ADVISOR_FIRST/LIMITED_INFORMATION inline CTA copy restored verbatim in `ReportView.tsx`'s `inlineConversionCopy()`, replacing the Phase 5.2 paraphrase. ADVISOR_FIRST: "Want help reviewing this decision carefully?" / "Pathways can help organize the questions, information, and next steps before you choose a direction." LIMITED_INFORMATION: "Want help getting clearer?" / "Pathways can help turn these open questions into a clearer set of options and next steps." PERSONALIZED unchanged. `report.actions.primary` and `src/lib/report/cta.ts` untouched. | Phase 5.2's own copy was an independent paraphrase written without the actually-approved wording in hand; the owner explicitly authorized and required the exact approved strings in Phase 5.2b. | `src/components/report/ReportView.tsx` | RESOLVED |
| DEC-S13 | `PathwayRoadmap.tsx`'s R06 layout now branches purely on the assembled data shape: `hasParallelPathway = steps.some((step) => step.stages.length > 1)`, computed from the same `groupStages()` output already used to render -- never from a persona, archetype, or fixture id. Purely sequential pathways get a new `.sequentialPathway` class enabling a horizontal desktop (>=900px) progression (evenly distributed numbered nodes, single horizontal connector line, plain labels, no card boxes); below 900px they fall back to the existing vertical spine stepper untouched. Pathways with a parallel group get a `.parallelPathway` marker class but no new CSS -- they keep the exact Phase 5.2a vertical split/rejoin treatment at every viewport, since they never match `.sequentialPathway`. The horizontal implementation keeps the numbered node a normal (non-absolutely-positioned) flex item, avoiding a repeat of DEC-S11's bug class. | Owner decision: sequential pathways should read as a compact, premium, product-like horizontal progression at desktop (matching pre-Phase-5.2 space efficiency without its squeeze bug); parallel pathways should keep the richer vertical treatment that makes GR12 legible, since forcing one layout to solve both information shapes was the root problem. | `src/components/report/PathwayRoadmap.tsx`, `.module.css` | RESOLVED |
| DEC-S14 | New Playwright coverage in `tests/e2e/report-visual-polish.spec.ts`: exact-copy assertions for all three contentStatus variants (with explicit assertions that the old Phase 5.2 paraphrases are absent), CTA operational-safety assertions (label + exact `/how-it-works` href) across GR03/GR09/GR15, horizontal-desktop/vertical-mobile bounding-box assertions for GR03/GR09/GR15/GR06, GR12's unchanged vertical split/rejoin behavior at desktop (branches side by side, subsequent stage below, not beside) and mobile (branches stacked within the group), and a permanent node/label non-overlap regression guard at 375 and 1440px for both a sequential and a parallel fixture -- a structural assertion, not only screenshot inspection, so a future regression of DEC-S11's bug class fails CI. | Section 17 of the Phase 5.2b instruction: protect against the exact bug class fixed in `1cc7e57`, not merely re-test that one instance. | `tests/e2e/report-visual-polish.spec.ts` | RESOLVED |

**Owner action (Phase 5.2b):** none required. Phase 4 educational decisions, Phase 5 report content, and the `DiscoveryReportDTO` shape were not changed; no AI, provider matching, or new framework/package/font was added; no merge to main occurred.
